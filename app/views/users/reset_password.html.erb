
<div class="row">
	<div class="span6 offset3">
		<%= form_for(@user) do |f| %>
			<%= f.label :email %>
			<%= f.text_field :email %>

			<%= f.submit "Send Email",class: "btn btn-large btn-primary",:controller => 'users', :active => 'create_password' %>
		<% end %>
	</div>
</div>